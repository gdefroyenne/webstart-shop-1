{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <div class="container">
        <h1>Page d'accueil</h1>

        {% for product in products %}
            <div class="card">
                <div class="card-body">
                    <div class="card-title">{{ product.name }}</div>
                    <div class="card-text">{{ product.description }}</div>
                    <div class="card-footer">
                        <div class="row">
                            <div class="col">{{ product.price / 100 }} €</div>
                            <div class="col"><button class="btn btn-primary">Ajouter un panier</button></div>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
